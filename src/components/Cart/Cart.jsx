import React from 'react';

const Cart = ({ cart }) => {
  console.log(cart);
  return (
    <div>
      <h1 className="font-medium">Order Summary</h1>
      <p>{}</p>
    </div>
  );
};

export default Cart;
